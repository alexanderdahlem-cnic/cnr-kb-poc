# build output (anchored to repo root so it does NOT match public/media/dist/,
# where self-hosted registry assets like country flags live)
/dist/
# dependencies
node_modules/
# astro
.astro/
# env & logs
.env
.env.*
*.log
npm-debug.log*
# os
.DS_Store
