# Archive

`pre-restructure/` holds a snapshot of `src/content/docs` taken **before** the
phase-1 restructuring migration re-run (TLD pages, remark directives, nav
order seeding, self-hosted media). It is kept outside `src/` so Astro does not
build it. Use it to browse or restore the previous page content if the new
structure drops something.
